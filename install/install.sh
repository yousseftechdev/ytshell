pip3 install reqiurements.txt
mkdir ~/.config/ytshell
cd ~/.config/ytshell
touch commands.txt
touch history.txt
touch theme.txt
echo 'help, mc, neofun, fuckyou, gc, dev, cd, mkcd, setrepo, uprepo, sp, url, cleanup, lsc, brc, zrc, exit, touch, rm, mv, cp, grep, find, cat, echo, date, pwd, ai, ask, vsc, vsci, history' > commands.txt
